package main

// main.go — Auth service entrypoint.
// BLOCKED until M5 gate passes. Source: IMPLEMENTATION_PLAN_V1 §BLOCK 1.
// M5 deliverables: SigV4 request verification, API key management.

import "log/slog"

func main() {
	slog.Info("auth-service: blocked until M5 gate — do not implement")
}
